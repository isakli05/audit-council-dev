# AUCDEV-023 S1 RB-001 L1 DRIVER-VARIANT REMEDIATION

- **Authority**: `AUCDEV-023-S1-RB001-L1-DRIVER-VARIANT-REMEDIATION-20260923-01` (NARROW IMPLEMENTATION REMEDIATION against the already-frozen L1 source candidate; driver-side ONLY)
- **Session role**: bounded driver-variant remediator + validation expander + record publisher ONLY — NOT Auditor-A/B, NOT the Control Room decision-maker, NOT an execution controller, NOT a boundary-launcher/EBS/test-reference-model/package/binding/event-generation/replacement-first-pass/qualification/installation authority. ZERO provider/model/frontier execution, ZERO auditor execution, ZERO credential read, ZERO report-substance read (the Auditor-A frozen report `ba8a29a1…`/23727 B/0444 remains sealed and was never opened). No L1 redesign; the accepted boundary candidate was NOT modified.
- **Date**: 2026-09-23 (Europe/Istanbul)
- **Resulting candidate label**: `L1_IMPLEMENTATION_REMEDIATED_AT_SOURCE_CANDIDATE_STRENGTH` — a SOURCE-CANDIDATE strength fact ONLY; NOT an audit PASS, NOT a qualification PASS.

## 1. Exact live baseline (verified before mutation)

- Live GitHub master at bootstrap (`git ls-remote`), re-verified EXACT: `7af938fa63af0abd3b08f7e4b176afaf0ecd6278`
- Root tree `98eb4badd5d57f22d3ddca692e2356543807dec1`; sole parent `efa8173ea2eec4f8f905738cd3b110d902d7bfa8`
- Canonical blobs at that SHA: CURRENT-STATE `5f8229a7e644bc33e97618313576ca0c6bea23d0`; BACKLOG `665bf0ed7e63a263d6c370c1be17ad9a196d82ac`; predecessor implementation record `80776182e8aa5eb74cbc87173cdd5f86291a8bf2`
- Protected trees byte-unchanged: bootstrap-supervisor `732b8def9f22d7c466ce77f3d3049da53bfff3d0`; qualification-harness `5b8d5e5465923740470ff63ed9b8683f257a3787`; skill `c792933a862d9a5434681a88d183470dd8b15d2f`
- Working-tree drift recorded without alteration (41 untracked entries, all preserved untracked).

## 2. Exact predecessor implementation handoff (independently re-verified)

`AUCDEV-023-S1-RB001-L1-IMPLEMENTATION-HANDOFF.tar.gz` — outer SHA-256 `6434988bd305621ca5954ac999962d4cbd1ef13a4454b8acab614946e61d019d`, 838661 B; census 47 members = 36 regular + 11 directories, 0 unsafe/traversal/duplicates/symlinks/hardlinks/special; exactly one SHA256SUMS 35 rows 35/35 PASS zero missing zero unlisted; packaged canonical records byte-equal to live Git blobs (`80776182`/`5f8229a7`/`665bf0ed`).

## 3. Predecessor source-candidate identities

| Artifact | SHA-256 | Bytes | Status |
|---|---|---|---|
| L1 boundary candidate | `011a87139254b7199ec55566074ea4da2324c7986337313d30c50670be403cfa` | 41270 | BYTE-UNCHANGED by this task (frozen; launcher_version `S1-PREP002-REM2-2`) |
| Predecessor driver (remediation base) | `cd4608a972bb3d1023f6a6bf8283189a8e7658bb58ca4bc9b46704f8d7fb617f` | 139721 | superseded by the successor below |
| Wrapper | `17e0abcd34303d0defda2326587cb80550872e8ebdb55b1055fb880e6d06534c` | 2836 | BYTE-UNCHANGED |
| Normative reference classifier | `d3066e8199afb26190e123a5d5f44e46194e541fd730f0795c413250426297e6` | 19869 | BYTE-UNCHANGED (never modified; re-run against) |
| Vendored frozen bwrap | `01fb705f067bd5365b63d8ad2323a61c8d007733ca5e649437e086f3fb9935d8` | 529776 | BYTE-UNCHANGED; NOT regenerated; NOT executed as a fixture gate (see §10) |

## 4. Control Room findings (input to this remediation)

- **`AUCDEV023-CR-S1-RB001-L1-IMPL-RB-001`** — `EARLY_RC3_PROGRESSIVE_PREFIXES_REJECTED_BY_DRIVER_VALIDATOR` (HARNESS/PROTOCOL IMPLEMENTATION DEFECT / OBSERVED SOURCE FACT / IMPLEMENTATION-BLOCKING): the candidate boundary initializes metadata with `launcher, launcher_version, role, attempt, event, credential_printed` and progressively assigns `auditor_executable_inode_checked`, `auditor_executable_is_frozen_package_payload`, `invocation_sha256`, `staging_dir`, `output_name`; any Exception emits the accumulated key-set plus `error_class=LAUNCHER_FAILURE` and returns rc=3. The predecessor driver validator required `staging_dir`/`output_name`/`invocation_sha256` UNCONDITIONALLY before accepting any pre-composition form, so legitimate earlier contiguous prefixes were rejected as generic `REJECTED_FAIL_CLOSED` — contradicting the accepted outcome-discriminated schema.
- **`AUCDEV023-CR-S1-RB001-L1-IMPL-RB-002`** — `EBS_EXEC_FAILURE_VARIANT_COLLAPSED_TO_GENERIC_METADATA_REJECTION` (HARNESS/PROTOCOL IMPLEMENTATION DEFECT / OBSERVED SOURCE FACT / IMPLEMENTATION-BLOCKING): the accepted live EBS records `exec_failed = (fail_byte == b"E")` and returns parsed `metadata or {}`; a boundary-launcher EXEC FAILURE legitimately reaches the driver as `expected.exec_failed == True` with `metadata == {}` — a separate EBS outcome variant (reference model: `EBS_BOUNDARY_EXEC_FAILURE` / `client_exec_reached=false` / `PROVEN_FALSE`). The predecessor validator did not recognize this variant before generic identity validation and collapsed it into `REJECTED_FAIL_CLOSED`.

## 5. Exact progressive rc=3 prefix state machine (RB-001 remediation)

Mechanically reconstructed from the EXACT candidate boundary source (`011a8713…`). Raising sites exist ONLY: (a) before the first progressive assignment (credential-fd read, invocation-fd read, `json.loads`, `resolve_auditor_path`), (b) at `os.makedirs` between `invocation_sha256` and `staging_dir`, and (c) throughout the composition phase after `output_name` (memfd creation, composition build, pipe/normalization, `subprocess.run` OSError, closes, drain). Adjacent assignments with NO raising site between them (`inode_checked → frozen_payload → invocation_sha256`; `staging_dir → output_name`) make every intermediate subset unreachable, and NO raising site exists after `client_returncode` is assigned (`os.path.isfile` swallows OSError/ValueError; the classifier is pure), so no post-composition partial prefix is producible. Exactly THREE contiguous key-sets are mechanically reachable:

| Variant class | Progressive keys at the failure point | Persisted `metadata_validation` token |
|---|---|---|
| `V-OUTER-PRE-COMPOSITION/INITIAL` | (none — base six keys only) | `ACCEPTED_PRECOMPOSITION_INITIAL` |
| `V-OUTER-PRE-COMPOSITION/IDENTITY_ESTABLISHED` | inode_checked + frozen_payload + invocation_sha256 | `ACCEPTED_PRECOMPOSITION_IDENTITY_ESTABLISHED` |
| `V-OUTER-PRE-COMPOSITION/STAGING_READY` | the above + staging_dir + output_name | `ACCEPTED_PRECOMPOSITION_STAGING_READY` |

Per-class contract enforced by the successor validator: required keys = base identity + the class set exactly; allowed keys = those + `error_class`; forbidden = every later/unrelated key (`PRECOMPOSITION_UNEXPECTED_KEY_*`, including `error`, `report_present`, `elapsed_seconds`); `error_class` MUST equal `LAUNCHER_FAILURE` (`PRECOMPOSITION_ERROR_CLASS_REQUIRED`); expected returncode MUST be 3 (`PRECOMPOSITION_RETURNCODE_NOT_3`); `auditor_executable_inode_checked` MUST be `True` when present (the source assigns the constant); grammar+identity enforced for every present progressive field (64-hex + recomputed-equality for `invocation_sha256`; equality for `staging_dir`/`output_name`); unreachable contiguous subsets (`PRECOMPOSITION_PREFIX_UNREACHABLE`) and gapped subsets (`PRECOMPOSITION_NONCONTIGUOUS_PREFIX`) fail closed; `client_returncode` or any exec-status field in a pre-composition variant fails closed (`EXEC_FIELDS_IN_PRECOMPOSITION_FORM` / `COMPLETED_FORM_MISSING_*`). NOT weakened anywhere: `credential_printed` escalation, unknown-key rejection, raw-error prohibition (`ERROR_CLASS_NOT_FINITE` + canary tests), identity equality for present fields, token/schema rejection, and the L1-only requirement for completed post-composition forms are all preserved (83/83 predecessor tests green). The accepted prefix classes persist the honest absent-token collapse (`EXEC_STATUS_PROTOCOL_VIOLATION` / `client_exec_reached=None` / `UNDETERMINED`) — `client_exec_reached` NEVER becomes true in this family. Documented expectation corrections (both separately justified under section 11 of the authority): (1) the predecessor single generic token `ACCEPTED_PRECOMPOSITION_FAILURE` collapsed three distinct legal variants — the discriminator now persists one token per accepted family; (2) the predecessor regression's "full prefix" fixture omitted the two auditor flags (a gapped, mechanically unreachable shape the predecessor validator wrongly accepted) and paired the form with expected returncode 137 — the corrected fixture is the true contiguous STAGING_READY shape with expected returncode 3.

## 6. EBS exec-failure outcome variant (RB-002 remediation)

Independently re-verified against the EXACT live EBS source (blob `063b6ce1f4c726bd6ba809f605a115511667fb09`): `_child_setup` wraps setsid/fd-remap/close-all/`fd_exec` in `except BaseException` → writes `b"E"` on the `FD_CLOEXEC` fail pipe (FAIL_FD=4) and `_exit(CHILD_EXIT_EXEC_FAIL=98)`; the EBS loop records `fail_byte` and returns `core = (exitcode, fail_byte == b"E", metadata or {})`; the exec-failed branch of `_report_and_terminalize` skips the report lifecycle entirely (`terminal_reason EXEC_FAILED_AFTER_CONSUMPTION`, `report_state ""`); the timeout path returns `exec_failed=False` explicitly — so `exec_failed=True` and `timed_out=True` are mutually exclusive and `exec_failed=True` arrives with `metadata == {}` EXACTLY. The successor validator adds the explicit top-level variant immediately after V-TIMEOUT (reference-model precedence: timeout first, then exec_failed) and BEFORE generic identity validation: `expected.exec_failed is True` + `metadata == {}` → `ACCEPTED_EBS_EXEC_FAILURE` with exec-evidence `EBS_BOUNDARY_EXEC_FAILURE` / `client_exec_reached=false` / `PROVEN_FALSE` / `proof_basis=[]` (exactly what the normative reference classifier permits for this input). Because the boundary exec never succeeded, NO boundary-generated value is trusted in this path. Fail-closed guards: any nonempty metadata (`EBS_EXEC_FAILURE_METADATA_NOT_EMPTY`), exec-status/token fields (`EBS_EXEC_FAILURE_WITH_EXEC_FIELDS`), a `report_present` claim (`EBS_EXEC_FAILURE_REPORT_CONTRADICTION`), or an EBS report-present state contradiction (`EBS_EXEC_FAILURE_REPORT_STATE_CONTRADICTION`). A boundary process that RAN and returned nonzero (completed form with `client_returncode`) remains a composition outcome (`V-COMPOSITION/L1-COMPLETED`, incl. the operator-accepted `EXEC_CALL_ATTEMPTED_TRANSITION_AMBIGUOUS` residual cell) and is NEVER reinterpreted as `exec_failed=true` — that flag belongs to the EBS boundary-exec failure only (test `drv/var/completed-nonzero-rc-stays-composition`).

## 7. Full outcome-variant model (successor validator)

`V-TIMEOUT` (`TIMEOUT_DISCARDED`) → `V-EBS-EXEC-FAILURE` (`ACCEPTED_EBS_EXEC_FAILURE`) → `V-ARGV-REFUSAL` / `V-METADATA-OVERSIZE` (the EXACT frozen rc=2 two-key forms, byte-identical behavior) → `V-OUTER-PRE-COMPOSITION/<INITIAL|IDENTITY_ESTABLISHED|STAGING_READY>` → `V-COMPOSITION/L1-COMPLETED` (`ACCEPTED_L1`; now additionally forbids `error_class` in the L1 completed form — the success path never assigns it; variant-mixing shapes fail closed) → `V-LEGACY-COMPLETED` (`ACCEPTED_LEGACY`, unchanged) → everything else `V-REJECTED-FAIL-CLOSED`. The finite token inventory is persisted as `ACCEPTED_METADATA_VALIDATION_TOKENS` (exactly nine accepted tokens) and the call-site conformance gate consumes that single inventory.

## 8. Successor driver identity + exact diff

- Predecessor driver: `cd4608a972bb3d1023f6a6bf8283189a8e7658bb58ca4bc9b46704f8d7fb617f` / 139721 B
- **Successor driver: `858b825da55eb04abf127463db92721b7ee44c6323a3316b7382683b0dfddf25` / 149121 B**
- Exact unified diff (predecessor candidate → successor): `022e79cbedcce28b9ea687e52a2c144a2378c28ca17f371859c4fbc6d8016afb` / 15928 B / **+185 / −20** (workspace `diffs/driver.diff`)
- Boundary diff: EMPTY (`e3b0c442…`, 0 B) — byte-identity witness; NO source changes after final hashes; the complete acceptance suite re-ran green on the frozen bytes with identical hashes before and after.

## 9. Validation results (all suites re-run on the frozen successor bytes)

- **Differential vs the EXACT normative reference `d3066e81…`**: 17021 checks / 0 failures — prior checks 17021, added 0, total 17021 (the remediation is outcome-envelope validation; `classify_exec_stage_l1` and `_exec_evidence_record` are source-identical to the predecessor candidate, proven mechanically by invariant `inv/15a`; predecessor coverage not reduced).
- **Predecessor design-validation suite**: 61/61 PASS (re-run unchanged against the same reference bytes).
- **Implementation matrix**: 119 / 119 PASS, 0 failures = predecessor 83 (all still green, including the vendored-bwrap integration cases) + **36 added** (three per-prefix acceptance tests with distinct tokens and `client_exec_reached is None`; per-prefix present-field identity tests; skipped-middle-field, later-without-predecessor, three unreachable-contiguous subsets, wrong-type, wrong-identity, raw-error-text canary, error-class wrong/absent, credential escalation, expected-rc≠3, unexpected/later keys, exec-fields-in-prefix, client_returncode-without-completed-fields; the six §9 EBS exec-failure cases + report-state contradiction + completed-nonzero-rc-stays-composition; variant-token inventory and prefix-table exactness).
- **Source invariants**: 41 / 41 PASS = predecessor 31 + **10 added** (`inv/13a` prefix-class table exact; `inv/13b` every reachable prefix → exactly one accepted variant; `inv/13c` exhaustive 32-subset enumeration — exactly the three class key-sets accepted, the other 29 all fail closed; `inv/14a` exec_failed+{}→EBS variant; `inv/14b` exec_failed+exec-fields/report-claims rejected; `inv/15a` classifier+renderer source-identical to predecessor; `inv/15b` completed-L1 validation view identical to predecessor; `inv/16a` boundary candidate byte-unchanged; `inv/16b` variant-token inventory exact; `identity/predecessor-candidates-exact`).

## 10. Vendored-bwrap evidence disposition

`VENDORED_BWRAP_FIXTURE_EVIDENCE_REUSED_WITH_EXACT_IDENTITY_REVERIFICATION` — driver-only remediation introduces NO new mechanical dependency on bwrap behavior; the fixture gate was NOT re-run (no ceremony). Read-only reverified: fixture results JSON + identity record reused from the verified handoff; the vendored binary re-hashed EXACT (`01fb705f…`/529776) at the workspace copy AND both deployed package paths (invariants `inv/12-bwrap-a/b`); the candidate boundary identity re-verified byte-exact (`inv/16a`); the predecessor acceptance record (`AUCDEV-023-S1-RB001-L1-IMPLEMENTATION.md`, blob `80776182…`) verified at the baseline. The mandated §11 implementation-matrix re-run includes the SAME vendored-engine integration cases as the predecessor (unchanged dependency, not a new one).

## 11. Blast-radius status / non-mutation

Boundary BYTE-UNCHANGED (`011a8713…`/41270); wrapper BYTE-UNCHANGED (`17e0abcd…`); EBS byte-unchanged (blobs `063b6ce1…`/`18f1cc60…`; `EBS_CHANGE_NOT_REQUIRED` preserved); reference classifier byte-unchanged; W1/W2 ABSENT (invariants `inv/11*`); deployed event / historical event backup / EXEC-05 state / package trees / canonical protected source trees NOT mutated; packages regenerated = NO; MANIFESTs regenerated = NO; bindings regenerated = NO; new event = NO; replacement execution = NONE; auditor/provider execution = NONE; qualification = NONE; installation = NONE. Candidate source bytes live ONLY in the isolated workspace `/home/isa/aucdev023-s1-rb001-l1-driver-remediation-20260923-01/` and the generated-LAST handoff.

## 12. Finding dispositions

- **`AUCDEV023-CR-S1-RB001-L1-IMPL-RB-001` = CLOSED_AT_IMPLEMENTATION_STRENGTH** — exact mechanical reason: the unconditional `staging_dir`/`output_name`/`invocation_sha256` requirement (the exact rejection cause in the predecessor source) is replaced by the per-variant prefix dispatcher; all three mechanically reachable rc=3 contiguous prefixes are accepted as distinct variants (`ACCEPTED_PRECOMPOSITION_*`) while every one of the 29 non-producible subsets fails closed (exhaustively enumerated, `inv/13c`); each present progressive field is still grammar+identity validated; `error_class=LAUNCHER_FAILURE` and expected rc=3 are enforced; nothing from the do-not-weaken list was weakened (83/83 predecessor tests green).
- **`AUCDEV023-CR-S1-RB001-L1-IMPL-RB-002` = CLOSED_AT_IMPLEMENTATION_STRENGTH** — exact mechanical reason: the explicit top-level `exec_failed` variant now precedes generic identity validation, verified against the exact EBS source; the only mechanically supported form (`metadata == {}`) is accepted as `EBS_BOUNDARY_EXEC_FAILURE`/false/`PROVEN_FALSE`/`[]` per the normative reference; nonempty metadata, exec-status fields, token claims, `report_present` claims, and report-present state contradictions all fail closed; nonzero-rc completed boundaries remain composition outcomes.

## 13. Resulting candidate state + authority barriers

Resulting L1 candidate state: `L1_IMPLEMENTATION_REMEDIATED_AT_SOURCE_CANDIDATE_STRENGTH` (boundary `011a8713…` unchanged + successor driver `858b825d…`; source-candidate strength ONLY — NOT execution-ready, NOT package-bound). Historical finding `AUCDEV023-CR-S1-EXEC05-RB-001` (RB-001) remains OPEN with its historical classification COMPLETENESS LIMITATION / FAILURE-DIAGNOSTIC EVIDENCE GAP / OBSERVED FACT / ROOT_CAUSE_UNRESOLVED / NO_PRODUCT_DEFECT_CONCLUSION_YET unchanged, the operator-accepted residual (`SE`+positive-nonzero-rc+report-absent = `EXEC_CALL_ATTEMPTED_TRANSITION_AMBIGUOUS`/null/UNDETERMINED) recorded separately and still implemented honestly; W1/W2 remain DEFERRED/ABSENT. EXEC-05 authority `AUCDEV-023-S1-FIRSTPASS-EXEC-20260922-05` remains CLOSED / NO_RERUN / NON-TRANSFERABLE with budget 2/2 charged fail-closed and barrier CLOSED_PENDING_CONTROL_ROOM_MECHANICAL_READBACK; attempts `evt-79182989824ce966-A-01/-B-01` remain TERMINAL; Auditor-A report substance remains sealed/unread; AUCDEV-023 remains P1 / READY / NOT DONE with NO backlog count/status change (READY 9 / OPEN 7 / BLOCKED 3 = 19 open; P0 2 / P1 7 / P2 11). Any future first-pass still requires a future NEW EVENT generation with fresh A/B identities + full successor preparation + Control Room readback + NEW explicit operator authority.

## 14. Next action (EXACTLY ONE)

CONTROL ROOM VERIFICATION OF THE REMEDIATED L1 SOURCE CANDIDATE (boundary unchanged + successor driver `858b825d…`, the exact prefix state machine, the EBS exec-failure variant, the expanded suites, and the generated-LAST handoff) BEFORE ANY PACKAGE / MANIFEST / BINDING / EVENT PREPARATION AUTHORITY.
